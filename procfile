# This procfile may be used in production, if you are deploying your app at Heroku. This is the way they recomend setting up the Unicorn server for production.

web: bundle exec unicorn -p $PORT -c ./config/unicorn.rb

